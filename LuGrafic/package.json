{
  "name": "lugrafic",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "set NODE_OPTIONS=--max-old-space-size=4096 && vite",
    "build": "set NODE_OPTIONS=--max-old-space-size=4096 && tsc && vite build",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "setup:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/rebuild-setup.ps1",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/plotly.js": "^3.0.13",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "mathjs": "^15.2.0",
    "plotly.js-dist-min": "^4.1.0"
  }
}
